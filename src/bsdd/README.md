# bsdd

A library to interact with the buildingSMART Data Dictionary (bSDD) API.
This package is totally independent from other packages in IfcOpenShell and requires only a python installation to run.
